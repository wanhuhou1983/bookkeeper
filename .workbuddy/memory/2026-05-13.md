# 2026-05-13

## 微信记账小程序 MVP 完成

### 项目位置
- 后端: `C:\Users\linhu\WorkBuddy\2026-05-13-task-15\backend\`
- 前端小程序: `C:\Users\linhu\WorkBuddy\2026-05-13-task-15\miniprogram\`

### 技术栈
- 后端: FastAPI + SQLAlchemy 2.0 + PostgreSQL 16 (localhost:5432, 密码 linhu50115)
- 前端: 微信小程序原生 + Vant Weapp
- 数据库: bookkeeper (9张表: users, records, accounts, categories, channels, banks, saved_searches, composite_stats, composite_stat_items)

### 核心功能
1. 5 Tab导航: 支出/收入/统计/拍照/设置
2. 收支记录CRUD + 复制(日期改今日) + 转换(支出↔收入)
3. 配置管理: 账本(5个)/类目(5个)/渠道(7个)/银行(4个) CRUD
4. 统计: 月度概览、筛选查询、保存统计项、复合统计(加减运算)
5. 渠道-银行联动: 选了渠道才能选银行
6. 拍照识图: 预留入口(MVP未实现)
7. 新用户自动种子数据

### 已验证API
- 所有CRUD API测试通过 (端口8900)
- 种子数据正确插入
- 统计概览返回正确汇总数据

### 微信小程序配置
- AppID: wx8cff51c9cc738ab0
- AppSecret: 645d3e1a880feebf25270c2fdad99f10 (已写入 .env，未提交到 GitHub)
- 后端端口: 8900

### GitHub 仓库
- https://github.com/wanhuhou1983/bookkeeper
- .gitignore 排除了 .env, node_modules, __pycache__, .workbuddy/

### 踩坑
- python-jose 要求JWT的sub必须是字符串，不能用int
- SQLAlchemy配置表(account/category/channel/bank)的user_id必须用ForeignKey("users.id")才能建立relationship
- PG连接密码是 linhu50115 (不是默认的postgres)
- Windows下taskkill有时无法杀python进程，需要用PowerShell的Stop-Process
- Vant Weapp 需要安装 npm 依赖后手动复制 dist/ 到 miniprogram_npm/@vant/weapp/
- WXML 不支持 .find() 箭头函数等 JS 高级语法，需要用 WXS 或在 JS 层预计算
- 微信开发者工具需要在「详情→本地设置」勾选「不校验合法域名」才能访问 localhost

### 代码审查修复 (commit 89786b2)
- SECRET_KEY 移除默认值，启动时未配置直接 ValueError 报错
- _calc_composite 添加 SavedSearch.user_id == stat.user_id 过滤，防跨用户数据泄露
- CORS 从 allow_origins=["*"] 改为从 CORS_ORIGINS 环境变量读取
- stats_overview recent_records 查询添加 joinedload(Record.account, Record.category) 消除 N+1
- records.py date_from/date_to 从 str 改为 date 类型，FastAPI 自动校验格式
- 清除所有 __pycache__ 的 git 追踪（30个文件），.gitignore 已有对应规则

### CSV 数据导入
- 来源: `C:\Users\linhu\Desktop\图图记账导出账单2026-05-13.csv` (2523条，2023-12~2026-05)
- 导入脚本: `backend/import_csv.py`
- CSV 列: 时间,类型,类别,子类别,币种,金额,备注,账户1,账户2,账本,标签,报销状态,转账手续费
- 映射: 类型→type(支出=1,收入=2), 类别→category(自动创建), 账户1→account(自动创建), 备注→note
- 新增 category: 微信/薪资/手续费/餐饮/支付宝/还信用卡/京东/红包/返利/还贷款/抖音/人情/积分兑换/刷单/投资收益/恋爱/投资亏损/银行 (共18个新类目)
- 新增 account: 羊毛+/期货+阿龙 (共2个新账户)
- 数据迁移到 user_id=1 (微信登录用户，openid=test_openid_001)
- 导入后验证: 2026-05 支出103,942 收入57,483 (API返回正确)
- 后端当前运行于端口8900 (uvicorn, 无--reload)
- SECRET_KEY 已更新为随机强密钥: pdum-WWpTdZeZgESxZOYLqK97ve38NNwRulQg2sbNjg

### 类别删除保护 (未分类兜底机制)
- Category 模型新增 `is_system` 字段 (Boolean, 默认False)
- 新增"未分类"系统类别 (is_system=True)，不可删除、不可改名
- 删除类别时: 自动将该类别下的所有记录迁移到"未分类"，然后软删除
- 创建类别时: 只检查活跃类别的名称重复，允许复用已软删除的名称（自动恢复）
- 更新类别时: 系统类别不可改名
- 迁移脚本: `backend/migrate_uncategorized.py` (为现有用户补"未分类"类别)
- ConfigOut schema 新增 `is_system` 字段，前端可据此禁用删除按钮
- Seed 数据: 新用户注册时自动创建"未分类"系统类别 (sort_order=999)

### Web 管理界面
- 访问 http://localhost:8900 即可打开
- 开发登录接口: POST /api/v1/auth/dev-login (使用 test_openid_001 账号，无需微信code)
- 页面功能: 概览(月度收支汇总+最近记录)、明细(筛选+分页)、统计(分类/账户柱状图)、设置(类别/账户/渠道CRUD)
- 静态文件位于 backend/static/index.html
- CORS 改为 allow_origins=["*"] 方便开发调试
- 启动后端需用 managed Python: `/c/Users/linhu/.workbuddy/binaries/python/versions/3.13.12/python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8900 --reload`
